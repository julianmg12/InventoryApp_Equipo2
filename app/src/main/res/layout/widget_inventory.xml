<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#CC000000"
    android:padding="12dp">

    <!-- Logo app -->
    <ImageView
        android:id="@+id/widget_logo"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_inventory_logo"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:contentDescription="Logo Inventory" />

    <!-- Texto Inventory debajo del logo -->
    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Inventory"
        android:textColor="#e7522e"
        android:textStyle="bold"
        android:textSize="14sp"
        android:layout_below="@id/widget_logo"
        android:layout_alignStart="@id/widget_logo"
        android:layout_marginTop="4dp" />

    <!-- ¿Cuánto tengo de inventario? arriba derecha -->
    <TextView
        android:id="@+id/widget_question"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="¿Cuánto tengo de inventario?"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true" />

    <!-- Icono ojo -->
    <ImageView
        android:id="@+id/widget_eye_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_below="@id/widget_question"
        android:layout_marginTop="4dp"
        android:contentDescription="Mostrar/ocultar saldo" />

    <!-- Saldo / **** -->
    <TextView
        android:id="@+id/widget_balance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="$ ****"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:layout_toStartOf="@id/widget_eye_icon"
        android:layout_alignBottom="@id/widget_eye_icon"
        android:layout_marginEnd="8dp" />

    <!-- Gestionar inventario (texto) -->
    <TextView
        android:id="@+id/widget_manage_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gestionar inventario"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="24dp" />

    <!-- Icono gestionar inventario -->
    <ImageView
        android:id="@+id/widget_manage_icon"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_toEndOf="@id/widget_manage_label"
        android:layout_alignBaseline="@id/widget_manage_label"
        android:layout_marginStart="4dp"
        android:src="@android:drawable/ic_menu_manage"
        android:tint="#FA8523"
        android:contentDescription="Ir a gestionar inventario" />

</RelativeLayout>
